{% extends 'base.html.twig' %}

{% block title %}Hello MainController!- {{ pizza }}{% endblock %}

{% block body %}
    <h2>Вы выбрали пиццу:</h2>
    {% block item %}
        <div class="row">
        <div class="col-md-2 text-center" >
            <h2 class="text-center" text-center>{{ pizza }}</h2>
            <div class="card mb-4 shadow-sm" width="400" >
                <span ><img src="{{ asset('upload/files/')}}{{pizza.image}} " </span>
                <div class="col-xs-2 buttons">

                    </form>
                </div>
                <div> Цена: {{ pizza.price }}</div>
                <div class="card-body">
                    <p class="card-text">{{ pizza.description|raw }}</p>
                    <div class="d-flex justify-content-between align-items-center">
                        <div class="btn-group ">
                            {{ form_start(form) }}
                            {{ form_widget(form.pizzaId, {'attr': {'value':pizza.id }}) }}
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


    {% endblock %}
{% endblock %}
